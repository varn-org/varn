@PACKAGE_INIT@

# the varn shared library links all of its dependencies privately, so a consumer only needs this target.
include("${CMAKE_CURRENT_LIST_DIR}/varnTargets.cmake")

check_required_components(varn)
